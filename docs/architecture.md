# Flux: architecture

Status: draft v1, 2026-08-28.

## Overview

```
┌──────────────── box (VPS / container) ────────────────┐
│                                                        │
│  claude ──stdin/stdout──┐                              │
│  pi     ──stdin/stdout──┤   daemon                     │
│  ~/.claude/…/*.jsonl ───┤   ├─ adapters (read + write) │
│  git worktrees ─────────┤   ├─ event log (sqlite)      │
│  flux tools (mcp, ext) ─┘   ├─ git / fs service        │
│                             └─ one outbound WSS ───────┼──┐
└────────────────────────────────────────────────────────┘  │
                                                            │ E2E encrypted frames
┌──────────────── operator's VPS ───────────────┐           │
│  relay: WebSocket forwarder, room = H(boxPub) │◄──────────┘
│         serves PWA static files               │◄──────────┐
│         holds Web Push subscriptions          │           │
└───────────────────────────────────────────────┘           │
                                                            │
┌──────────────── phone / laptop ───────────────┐           │
│  pwa: Vue 3, CodeMirror 6, IndexedDB cache    │───────────┘
└───────────────────────────────────────────────┘
```

Three deployables, one shared package (`packages/protocol`) holding the wire types, codecs and crypto helpers used by all three.

## Trust model

- **Box** is fully trusted. It runs agents with permissions bypassed. Anyone who can execute on the box owns everything.
- **PWA device** is trusted once paired. Pairing proves possession of a one-time secret shown on the box's terminal. Trust is per device (per client keypair) and revocable from the box.
- **Relay** is untrusted. It sees: room ids (hash of a public key), connection timing, frame sizes, and Web Push endpoints. It never sees plaintext, keys, repo names, or which device is which beyond "host" and "guest".
- **Network** is untrusted. All frames are AES-GCM under a per-connection key derived from X25519 (static+ephemeral) and the handshake transcript, see `protocol.md` § 3; a plaintext WebSocket is refused off loopback (`insecure_transport`).

Compromise of the relay yields traffic analysis only. Compromise of a paired device yields the box. That is the intended trade for a single-operator tool; write it down so nobody is surprised later.

## Daemon

Single Node 24 process. Responsibilities, in order of importance:

1. **Event log.** One SQLite database (`node:sqlite`), table `events(session, seq, ts, type, payload, parent)`, primary key `(session, seq)`; `parent` (added in place to a database that predates it) is the Agent call a subagent's event belongs to, null for the rest. Append-only. `seq` is per session, gapless, starts at 1. This is the source of truth for everything the PWA shows. Streaming text deltas are _not_ logged; they go over an ephemeral channel and the final message is logged once.
2. **Sessions.** A session = one agent process + one worktree + one event log stream. Session state is a small row: repo, worktree path, branch, agent kind, agent-native session id (for resume), state (`idle | running | waiting_user | ended`). On daemon restart, sessions are reattached using the agent's own resume mechanism. A session ends from the device (`session-lifecycle.ts`, ADR 0018): `sessions.clear` settles the agent's open asks as aborted (through the ask registry, so the waiting tool gets its result and the answer is logged before the agent goes), closes the agent, forgets the agent-native session id (and pi's session file, so pi starts a new one under the same `--session-id`) and logs `session.cleared`, so the next message spawns a fresh context in the same worktree; `sessions.archive` closes the agent and hides the session, optionally removing the worktree (refused as `dirty` while it holds uncommitted files or unpushed commits, unless told to discard) and then the branch; `sessions.unarchive` brings it back while its worktree exists. Archived sessions stay in the store and in `sessions.list`, flagged `archived` and `worktreeExists` (a `stat` at list time), so the device can offer Reopen only where there is something to reopen. `sessions.rename` sets the title (the tab's label; defaults to the branch at creation) and logs `session.renamed`, which is how every device's tab picks it up.
3. **Adapters.** Per agent kind, split into a read side and a write side (see below).
4. **Flux tools.** A stdio MCP server, spawned by the agent, that calls back into the daemon over a local Unix socket. Provides `flux_ask` and `flux_notify`. This is how "the agent needs you" happens; the agent's own interactive tools are absent in headless mode. pi has no MCP client, so for pi the same two tools are a pi extension that speaks to the same socket (ADR 0016).
5. **Git and fs service.** Worktree create/list/remove, status, diff (against branch base and against HEAD), show, log, file read, file write, and the operator's git actions: commit (all changes or chosen paths), push (never forced; the first push of a branch sets its upstream) and open a pull request through `gh` (`adr/0014`), which also logs `pr.published` so the PWA learns of a PR the same way whether the operator or the agent opened it. Executed by spawning `git` and `gh` with an argument vector and a timeout (`run-command.ts`); no git library, no shell strings. Reads cap at 1 MiB (`truncated`), carry a sha256 of the whole file, and call anything that is not valid UTF-8 binary. A write is atomic (temp file with the old mode, then rename), refuses any path that lexically or through a symlink leaves the worktree or has a `.git` segment, writes through an in-worktree symlink, and with `ifMatch` only lands when the file still has the hash it was read with (`conflict` otherwise). Writes to one file are queued on the box, so two devices cannot both win; an agent's own write between check and rename is the accepted gap.
6. **Transport.** One outbound WebSocket to the relay, reconnecting with backoff; `FLUX_RELAY_URL` must be `https://` unless it names loopback (`insecure_transport` at start, exit 2). Handshake and encryption per `protocol.md`: the channel keys bind both handshake hellos as sent, and the device's first frame is its key confirmation, so a channel whose first frame does not open is dropped rather than kept (ADR 0019). A device on another protocol version is answered with the box's version and given no channel. Multiplexes: event stream (replayable), ephemeral stream (deltas, presence), RPC (request/response). `create-device-channels` keeps one channel per completed handshake, grouped by device fingerprint, so several tabs of one browser profile coexist: an incoming frame is tried against that device's channels and opens on exactly one, `sendTo` and `broadcast` seal once per channel, revocation cuts every channel of the device, and `peers()` reports each device once. The relay never reports a guest leaving, so a dead tab's channel lingers until the device is past 8 channels (the relay's guest cap). Past it, channels still waiting for their first frame go first, oldest first, except that one younger than `confirmGraceMs` (2 s) is kept while the device holds no more than 8 pending channels (tabs restored together all handshake before any sends its first frame, and one must not evict another), so a device holds at most 16 channels. A confirmed channel is only displaced by a channel that has just confirmed, when the device really holds more working tabs than the relay admits: then the confirmed one heard from least recently is dropped (a live tab is heard from at least once a minute, its keepalive `hello`). A handshake replayed under a device's public key by another guest, or a tab reconnecting in a loop, therefore never evicts a working channel; a sustained flood of more than 8 replays per round trip can still keep a new tab from confirming, which is no more than that guest could do by filling the relay's guest slots. An unconfirmed channel older than `confirmWithinMs` (30 s) is dropped lazily on the device's next handshake or frame, no timer; all channels go when the socket does.
7. **Pairing.** `flux daemon` on first run generates the box keypair. `flux daemon` and `flux pair` print a QR containing `https://<relay>/#<boxPub>.<secret>` above the URL. The encoder is in-house (`src/qr/`, no dependency per ADR 0010): byte mode, error correction level M, versions 1 to 15, rendered with Unicode half blocks so it fits a terminal. Paired device public keys live in the daemon's SQLite, with when each was paired and last said hello. Any number of devices can pair while others are connected; revoking one (`devices.remove` from a device, or `flux devices rm`) deletes its key and push subscriptions and tells its live channel (`device.revoked`, `protocol.md` § 6) before dropping it.
8. **Lifecycle** (ADR 0017). `flux daemon` takes `<dataDir>/daemon.lock` (its pid; exit 3 if another live daemon holds it, a stale one is replaced), settles what the last daemon left (`ask` without `ask.answered` in a `running` or `waiting_user` session → `ask.answered aborted`; the session → `idle`, reason `daemon restarted`), then binds the control socket and connects. `stop()` is bounded: transport off, pending asks aborted, control connections destroyed, each agent closed by stdin EOF → SIGTERM → SIGKILL of its process group with a grace per stage, sessions caught mid-turn logged `idle`, stores closed, lock released. A second signal or a 10 s budget SIGKILLs every agent's group, drops the lock and exits at once (`abandon()`).
9. **Attachments** (ADR 0020). Files the operator sends with a message arrive in chunks over the channel (`attach.begin`, `attach.chunk`, `attach.end`, `protocol.md` § 7) and are stored by `create-attachment-store.ts` under `<dataDir>/attachments/<session>/<id>-<name>` (name reduced to `[A-Za-z0-9._-]`, path checked with `inside()`), streamed to a file handle with the sha256 computed as chunks land (`attachment-upload.ts`), rows in the `attachments` table. `agent.send.attachments` names them; the supervisor logs them on `msg.user`, lists each in the prompt as `Attached: <path> (<mime>, <size>)` (`render-attachments.ts`, composed with the refs and the reply quote by `render-prompt.ts`), and sends png, jpeg, gif and webp files within 5 MiB to the agent as image content blocks as well (`attachment-images.ts`; `spawn-claude.ts` puts them in the stream-json user message's content array, verified against `fixtures/claude/session-image-block`; `spawn-pi.ts` as the prompt's `images`). Cleanup is lazy: at start and before each `begin`, uploads not ended within 10 minutes and attachments not sent within 24 hours are deleted; sent ones stay until the session is deleted (`sessions.archive` with `removeWorktree` removes its directory; a plain archive keeps it).
10. **Settings.** Runtime settings the operator may change from the PWA (repositories directory, default agent, which events push) live in the `settings` table and override the environment's starting values; environment-only values (relay URL, data dir, push subject, agent binary) are reported read-only. The agent's global config (`~/.claude/CLAUDE.md`, `~/.claude/settings.json`) is read and written as raw text (temp file and rename), with `settings.json` checked to be a JSON object before it touches disk (ADR 0016).

**Versioning.** All three apps share one app version (semver), the root `package.json` `version`, stamped into each build at build time (the daemon and PWA read it as `FLUX_VERSION` through their `version.ts`; a dev fallback covers source and test runs). The daemon returns it from the `hello` RPC result so the device knows what the box can do; the PWA shows both versions read-only in Settings and offers **Update daemon** when the box is behind. Cutting a release — the tag-triggered CI, the signed daemon bundle, the relay image and the operator's sign-and-publish step — and the manifest format and how it is verified against the trusted ed25519 key set are the runbook in `releases.md`.

**Self-update** (ADR 0022, `src/update/`). `daemon.update { version }` (the PWA passes its own version) installs a newer release. The daemon validates the target with the shared `semver` (valid, strictly newer, at or above the 1.0.0 floor, not a source checkout) and refuses the rest with `unsupported`; otherwise it returns `{}` at once and runs `apply-update` off the RPC: emit `update.progress fetching`, fetch the five release assets from GitHub Releases (`fetch-release.ts`), emit `verifying` and check the signature and per-file hashes with the #41 verify core (`verify-manifest.ts`, trusted keys), emit `installing` and write the three verified `.mjs` files to a temp dir under the data dir then atomically rename each over its installed path (`distDir`, the siblings of the running `index.mjs`), then emit `restarting`, run the bounded shutdown (ADR 0017) and `process.exit(0)` for the supervisor to restart into the new code. Every phase is `update.progress`; any abort is `update.failed` (`download_failed` / `bad_signature` / `disk_error`), the running daemon untouched; success has no event — the channel drops, the device reconnects and reads the new `hello.version`. Every side effect (fetch, emit, exit, stop, `distDir`) is injected, so `apply-update` is unit-tested with an ephemeral signing key and no real network or process exit.

**Supervision** (ADR 0022 § 6, `src/service/`). The daemon stays a plain process; keeping it always-on, restarting it on crash, starting it on boot and — critically — restarting it after the self-update's clean `process.exit(0)` is the host's supervisor, installed by `flux service <install|uninstall|status>`. This is a CLI/host concern only: no relay URL, no Daemon instance, nothing on the wire (it dispatches in `index.ts` before `createDaemon`, like `flux pair`). It detects the host and writes the right manifest, tailored to the running install (the absolute node binary, the installed `index.mjs`, the invoking user, and the daemon's `FLUX_*`/`PATH` environment): on **Linux** a hardened systemd system unit modelled on `deploy/flux-daemon.service` (`Restart=always`, `RestartSec=5`, `RestartPreventExitStatus=2 3`, `WantedBy=multi-user.target`) — installed and `enable --now` as root, or staged under the data dir with the exact `sudo` commands printed when not root, never a silent escalation; on **macOS** a per-user LaunchAgent at `~/Library/LaunchAgents/com.flux.daemon.plist` (`RunAtLoad` + `KeepAlive`, no sudo) so the agents inherit that user's keychain, PATH and `claude`/`gh` logins (a headless Mac that must run before login needs a root LaunchDaemon instead, at the cost of the GUI session and keychain); and on a box with **no init system** (a typical devcontainer) a restart-loop wrapper script (`while true; do node …/index.mjs daemon; sleep 1; done`) to run under nohup or the container's restart policy, which is what makes exit-to-update safe there. The manifest _text_ is produced by pure renderers unit-tested on the generated string; every side effect (`writeFile`, `systemctl`/`launchctl`) and the host detection (`platform`, `isRoot`, `user`, whether systemd is present) is injected, so all three branches and root vs non-root are covered without running on three OSes. Failures are `DaemonError`; the operator runbook is `releases.md` § Supervision.

### Adapter: read side and write side

The read side must work regardless of how the agent was launched. The write side is an interface with more than one implementation. This split is deliberate and is the mitigation for the policy risk in `prd.md`.

**Claude Code, read side.** Two sources, same parser:

- stdout of `claude -p --output-format stream-json --verbose` (live)
- `~/.claude/projects/<slug>/<session_id>.jsonl` (recovery, and any session Flux did not start)

Both carry the same `assistant` / `user` message objects. Mapping (verified against Claude Code 2.1.251; subagents against `fixtures/claude/session-subagents`, where each line a subagent produced carries the spawning Agent call's id as `parent_tool_use_id` and Claude Code emits no stream events, no final text and no `result` for it):

| stream line                                                                                                | FluxEvent                                                                                                                                                                                        |
| ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `system/init`                                                                                              | `session.created` (also stores `session_id` for resume)                                                                                                                                          |
| `system/status` `requesting`                                                                               | `session.state running`                                                                                                                                                                          |
| `stream_event` `content_block_delta` text                                                                  | ephemeral delta, not logged                                                                                                                                                                      |
| `stream_event` `message_start`                                                                             | ephemeral `agent.context` (prompt size = `input + cache_creation + cache_read`, model; the supervisor adds the window when `context-window.ts` knows the model), not logged                      |
| `assistant` with `text` block                                                                              | `msg.assistant`                                                                                                                                                                                  |
| `assistant` with `tool_use` block                                                                          | `tool.start`                                                                                                                                                                                     |
| `user` with `tool_result` + `tool_use_result`                                                              | `tool.end`, and `files.changed` when `tool_use_result` reports a file write/edit                                                                                                                 |
| `result`                                                                                                   | `turn.ended` (cost, duration, token usage), `session.state idle`                                                                                                                                 |
| `rate_limit_event`                                                                                         | `rate_limit` (utilisation per window)                                                                                                                                                            |
| `stream_event` `content_block_start` thinking, `system/thinking_tokens`, that block's `content_block_stop` | ephemeral `agent.thinking` (on, count, off), not logged; the adapter throttles counts to one per 500 ms or 100 tokens                                                                            |
| `system/task_started`, `system/task_notification`                                                          | `task.started` (`agentType` from the line's `subagent_type`, else from the Agent call's input), `task.ended` (`summary` is the subagent's report, `tokens` its `usage.total_tokens`)             |
| any line with a non-null `parent_tool_use_id` (a subagent's)                                               | the same mapping, with `parent` set on every event; its deltas, thinking and `message_start` are dropped, since the streaming bubble, the indicator and the context reading are the main agent's |
| `user` with text content under a parent                                                                    | `msg.user` with `parent`: the subagent's prompt, which opens its chat. A top-level `user` text line stays `raw` (the operator's own messages are not echoed)                                     |
| `system/task_progress`                                                                                     | `task.progress` (what the task is doing now and its `usage.total_tokens` so far; the device's agents strip shows it in place of the description while the task runs)                             |
| `system/task_updated`                                                                                      | `raw`                                                                                                                                                                                            |
| `system/code_change_published`                                                                             | `pr.published` (the agent opened a PR with `gh`)                                                                                                                                                 |
| `system/vcs_state_changed`                                                                                 | ephemeral `vcs.changed`, not logged                                                                                                                                                              |
| `system/hook_response` with outcome ≠ success                                                              | `hook.failed` (stderr capped at 2 KiB)                                                                                                                                                           |
| successful hooks, other stream envelopes, anything else                                                    | `raw`                                                                                                                                                                                            |

**Claude Code, write side.** `AgentInput` interface: `send(text)`, `interrupt()`, `close()`. Implementations:

- `StreamJsonInput`: writes `{"type":"user","message":{"role":"user","content":"…"}}` lines to stdin (with image attachments, `content` is an array of a `text` block and base64 `image` blocks, ADR 0020) of a long-lived `claude -p --input-format stream-json --output-format stream-json --verbose --include-partial-messages --dangerously-skip-permissions [--resume <id>]` process. Default.
- `PtyInput`: drives interactive `claude` in a pseudo-terminal. Fallback if the headless path becomes unavailable. Also the path for agents that only have a TUI.

**pi.dev, both sides** (ADR 0016). One long-lived `pi --mode rpc --session-dir <data>/pi-sessions --session-id <flux session> --no-approve --no-extensions --no-skills --no-prompt-templates --extension <flux-pi-extension> --append-system-prompt …` per session (nothing from `~/.pi` is loaded; context files are), `--provider`/`--model` from `FLUX_PI_PROVIDER`/`FLUX_PI_MODEL` when set. The Flux session id is pi's session id, so a restart resumes with the same arguments. Write side: `{"type":"prompt","message":…,"streamingBehavior":"followUp"}` lines to stdin (image attachments as its `images` field, ADR 0020); interrupt is `{"type":"abort"}` and keeps the process (a flux_ask in flight is released as `ask.answered by: aborted`). The last 2000 bytes of pi's stderr go into the `ended` reason. Read side, the JSON lines on stdout (verified against pi 0.84.4; fixtures under `apps/daemon/test/fixtures/pi`):

| rpc line                                                                                                                                                    | FluxEvent                                                                                                                                                              |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `agent_start`                                                                                                                                               | `session.state running`                                                                                                                                                |
| `message_update` with `text_delta`                                                                                                                          | ephemeral delta, not logged                                                                                                                                            |
| `message_end`, role `assistant`, `text` blocks                                                                                                              | `msg.assistant`; usage and cost summed into the run; ephemeral `agent.context` from the message's input side (`input + cacheRead + cacheWrite`, the model), not logged |
| `message_end`, role `assistant`, `stopReason: error`                                                                                                        | `raw` with pi's `errorMessage`                                                                                                                                         |
| `tool_execution_start`                                                                                                                                      | `tool.start`                                                                                                                                                           |
| `tool_execution_end`                                                                                                                                        | `tool.end`, and `files.changed` after a successful `write`, `edit` or `bash`                                                                                           |
| `agent_settled`                                                                                                                                             | `turn.ended` (summed cost and tokens, `numTurns` = assistant messages, last `stopReason`), `session.state idle`                                                        |
| `response` with `success: false`                                                                                                                            | `raw`                                                                                                                                                                  |
| `auto_retry_start/end`, `compaction_start/end`, `summarization_retry_scheduled`, `extension_error`, `hook_error`, `extension_ui_request`                    | `raw` notice with only the telling fields (attempt, delay, reason, error, method); a dialog is also answered `cancelled`                                               |
| `response` ok, `turn_*`, `message_start`, user and tool-result `message_end`, `agent_end`, `tool_execution_update`, `queue_update`, `bash_execution_update` | dropped                                                                                                                                                                |
| anything else                                                                                                                                               | `raw`                                                                                                                                                                  |

pi's own tools are lower-case (`read`, `bash`, `edit`, `write`, `grep`, `find`, `ls`); `pi-tool-summary.ts` renders those and the Flux tools. No `durationMs`: the stream carries no end time.

### Flux tools (MCP, and a pi extension)

`apps/daemon` ships a tiny MCP stdio server binary. The daemon writes a per-session `.mcp.json` pointing at it and passes `--mcp-config` to the agent. For pi, which has no MCP client, the daemon ships the same two tools as a pi extension (`dist/flux-pi-extension.mjs`, passed with `--extension`) that calls the same control socket with the same request lines (ADR 0016). Tools:

- `flux_ask(question: string, options?: string[]) -> string`. Emits an `ask` event, blocks until the operator answers (from PWA or notification), returns the answer. Times out after a configurable period with a clear error so the agent can proceed sensibly.
- `flux_notify(summary: string, level: 'info' | 'done' | 'blocked')`. Emits `notify`. Non-blocking.

The daemon appends a short system prompt (`--append-system-prompt`) instructing the agent to prefer `flux_ask` over guessing on material decisions and to `flux_notify` when done or blocked. Because Flux owns these tools, the `ask` schema is stable across agents and Claude Code versions.

## Relay

Hono on Node 24. Two jobs:

1. `GET /*` serves the PWA build.
2. `GET /ws/:room` WebSocket. First frame declares role `host` or `guest`. One host per room; the second host is rejected. Frames from host fan out to all guests; frames from a guest go to the host only. Frames are opaque binary. No persistence, no logging of room ids beyond ephemeral metrics.
   The relay holds no state beyond live connections. Web Push is sent by the daemon directly (`adr/0013`).

Limits: max frame size, max guests per room, per-IP connection rate. All hard-coded initially. Behind a reverse proxy every socket is the proxy's address, so the deployment sets `FLUX_TRUST_PROXY=1` and the rate key is the last hop of `X-Forwarded-For` (`client-key.ts`); without the flag the header is ignored.

## PWA

Vue 3 (Composition API, `<script setup lang="ts">`), Vite, CodeMirror 6 with `@codemirror/merge` for diffs. IndexedDB holds the client keypair, the paired box (pubkey, relay URL), and a cache of the event log per session so the app renders instantly and then syncs.

Screens (P1): pair (camera via `BarcodeDetector`), sessions list / tabs, session view (chat + tool timeline), changes (file list → diff view with line comments and a comment tray; plus the P2 git actions: tick files, commit, push, open a PR and get its link), new session. P2 so far also: an editor for one worktree file, reached from the changes list, the diff view, or the file browser; and a file browser (Files in the session toolbar) that walks the whole worktree through `fs.list` (dirs first, then files, each alphabetical; a tappable breadcrumb), so any file can be opened, not only the changed ones. Settings (P2, built): paired devices with revoke, the box's runtime settings with the environment-only values shown read-only, and the agent's global config as two editors.

State: one reactive store for the app. It holds the connection (status, daemon name, last error, push state, rate-limit windows), the session list, the composer draft per session (text and attachments, `composers`), the fetched image thumbnails (`thumbs`, blob URLs by attachment id), and one log view per opened session with `events[]`, `lastSeq`, `streaming` (current delta buffer), `thinking` (the agent's open thinking block and token estimate, from `agent.thinking`), `context` (the context window in use for the open session, from `agent.context`: tokens and, when the box knows the model's window, that window) and `changes` (a counter bumped by `vcs.changed`). Pending comments, the open ask, the session's PR and its subagent tasks are derived from the events, not stored.

Layout of `apps/pwa/src`, as built:

- `client/`: connection, RPC client, session log, sync, pairing, storage. No Vue; runs in Node against `test/fake-relay.ts`. The connection refuses a relay origin that is not `https://` unless it is loopback (`insecure_transport`, before any socket opens), drops any frame that is not for its channel (the relay broadcasts the box's frames for every guest, other tabs of this device included), reports a box on another protocol version through `onError` (`bad_version`, "Box is on protocol 1; update it", shown as a connection error) while retrying at the full backoff, and calls `hello` once a minute while up, so the box never mistakes a quiet tab for a closed one. A `hello` that times out (the first one, the box having dropped an unconfirmed channel, or a keepalive) closes the socket for a fresh handshake; no other method's timeout does.
- `store/`: one reactive store (`createStore`) over storage plus connection: boot, pair, open a session (cache first, then `events.sync`), send, answer, comments, session list, push subscription, save a file, devices and settings (`settings-actions.ts`). Actions a view fires resolve to a boolean and put their failure in `state.error` for the status bar as `{ message, kind }` (`store-errors.ts`): an `action` error (a send, a git action, a settings save) clears itself after 8 s, at the next successful action, or when the operator dismisses it; a `connection` error (pairing refused, the box forgot this device, `hello` failed) stays until the box is back or it is dismissed, and navigating between screens never clears it. An action error that arrives while a connection error stands only covers it: when the action error goes, the connection error is shown again; dismissal clears both. The kind is set where the error is reported, never read off the message. The auto-clear timer is injected (`schedule`) so tests fire it; `saveFile` alone resolves to an outcome, because a `conflict` is the editor's to handle (it offers a reload or an overwrite) rather than the status bar's. `state.drafts` holds unsaved editor text per session and path, in memory only. A `device.revoked` notice naming this device, or a `not_paired` answer after pairing, forgets the stored keys and the cached logs and returns to the pair screen (`boxLink.unpair`). The push subscription is stored on the box after `hello` if permission is already granted (the Pair tap asks for it), otherwise the status bar offers "Enable notifications" so the permission dialog runs under a gesture. The offer only appears where push can work (`push/push-support.ts`: a service worker, a push manager, and not the dev server, which registers no worker); a prompted subscribe that fails rejects with a typed `ClientError` (`push_unsupported`, `push_denied`, `push_no_worker` after a bounded wait for the worker, `push_failed`) that the status bar shows as an action error, and `push_unsupported` withdraws the offer. The cache is append-only, in chunks of 256 events per storage key. `app-store.ts` binds it to IndexedDB and the native WebSocket. Pending comments, the open ask and the session's pull request are derived from the log (`pendingComments`, `openAsk`, `sessionPr`), never stored separately; a `session.cleared` marker ends any ask before it (`openAsk`) but not the pending comments, which are about the code and go to the fresh agent. The session lifecycle actions (`session-actions.ts`: send, clear, archive, unarchive, delete) refresh the list from the box; `deleteSession` alone resolves to an outcome, because a `dirty` refusal is the confirm's to handle. Attachments (`attachment-actions.ts`, ADR 0020): `attach` starts uploading each file at once (`client/upload-attachment.ts`: sha256 with `crypto.subtle.digest`, then `attach.begin`, 512 KiB chunks from `Blob.slice` encoded with `client/base64.ts`, `attach.end`), keeping the chip's state in `state.composers[session].attachments` and the `File` itself in the internals; `removeAttachment` deletes a finished upload on the box and stops a running one, which then deletes its own; `retryAttachment` starts a failed one over; `send` names the ready ids in `agent.send.attachments` and clears the draft once the box has the message. `loadThumbnails` fetches each image on a `msg.user` through `attach.read` pages into a blob URL under `state.thumbs`, once per id; `leave` revokes the session's.
- `router/`: a hand-rolled route switch (ADR 0004) over `history.pushState`: `/`, `/new`, `/settings`, `/s/<id>`, `/s/<id>/changes`, `/s/<id>/files?path=…`, `/s/<id>/diff?path=…`, `/s/<id>/edit?path=…[&dir=…]` (`dir` is the browser directory a file was opened from, so the editor's back returns there). The URL fragment is reserved for pairing links.
- `components/`: `Pair`, `Shell` (tabs, gear to settings, routed screen, status bar), `SessionTabs` (one tab per session in creation order, `createdAt` then id, never re-sorted on activity; the state dot and a count of events since the tab was last active show where the work is, and the strip scrolls only when the operator selects or creates a session), `SessionView` (`SessionToolbar`: branch, PR link, Stop, Files, Changes and `SessionMenu`, a `aria-haspopup="menu"` button offering Clear context, Archive and Delete…, the last through `DeleteConfirm`: remove worktree ticked, delete branch unticked, and on a `dirty` refusal the box's counts with a Discard changes confirm; archiving or deleting leaves for the list screen; then `AgentStrip` while it has a task row (a lone `main` would only repeat the toolbar): one row per task from `store/session-tasks` (`main` first, then every `task.started` in order with its nested tasks indented under it: a spinner while it runs, `○` once ended, `✗` when failed or interrupted, the `agentType` and, on one line, the latest `task.progress` note while it runs, else the description, at most four rows tall before it scrolls; the strip lists current tasks only: those still running and those that ended in the current turn, a turn starting at each top-level `msg.user` or `session.cleared`, with a nested task following its parent, and earlier turns' tasks leave the strip when the operator sends the next message, staying reachable from their `task.started` note, the open chat's row kept in the strip while it is viewed so Back has a place). It sits under the toolbar rather than above the composer because the composer's edge is where the phone keyboard lands, and a strip there would jump with it. Tapping a row switches the timeline to that agent's chat (`composables/useSessionTimeline`: the events whose `parent` is the task's `toolUseId`, the last 200 with a Show earlier button, no virtualisation), and `main` returns; while a subagent's chat is open the composer is replaced by a "Messages go to main" note with a Back button, since there is no way to talk to a subagent; the timeline, streaming text or, while the agent is inside a thinking block, a spinner beside a "Thinking… ~1.2k tokens" indicator, a PR link in the header once the log has a `pr.published`, `AskCard` (shown in every view: the question is the session's), `Composer` (`CommentTray`, `AttachmentChips` and the message box; files arrive by the `+` button's hidden file input, by a drop on the bottom bar, composer and status bar together, through `composables/useFileDrop`, which listens on the document, highlights the bar with a `drop-target` class and swallows a drop anywhere else so it cannot navigate the tab, or by a paste of files on the textarea, a text paste being left to the browser; each file is a chip with a thumbnail or an icon, its size, a progress bar while it uploads, the error and Retry when it failed, and × to remove it; Send waits until every upload is ready; the draft, text and files, lives in the store so leaving the session keeps it); the main timeline skips events with a `parent`, `raw`, `rate_limit` and `task.progress`, which stay in the log: a subagent's rows belong to its own chat, hooks and streaming envelopes would otherwise put half a dozen bare rows around every reply, and the status bar is where rate limits show; a task is open from its `task.started` until its `task.ended`, or until the session leaves `running` (`session.state` idle or ended, `session.cleared`), when it shows as interrupted, all derived from log rows with nothing persisted; the timeline follows new content only while the operator is within 32 px of the bottom, otherwise the scroll position stays put and a `↓ N new` pill offers to catch up, counted for the open chat only and only for rows newer than the last shown (Show earlier and a chat switch count nothing), and sending, answering, opening a session or switching chats always jumps to the end: `composables/useTailScroll`), `ChangesView` (file list with tick boxes and an Edit button per file, refetched when a `vcs.changed` notice arrives, embedding `GitActions`: commit box, push, open PR with the title seeded from the branch's newest Conventional Commit subject, else the newest commit, else the session title, until the operator edits it; a muted non-blocking hint when the title would fail a Conventional Commits check, since the squash commit takes the title; the PR link taken from the log), `FilesView` (the worktree file browser reached from the toolbar's Files button: `fs.list` for the current directory kept in the URL as `path`, dirs before files each alphabetical, a tappable breadcrumb and a `‹` up, tapping a dir descends and tapping a file opens it in `EditView` carrying the directory it was opened from so the editor's back returns there; loading, empty and failed-listing notices), `DiffView`, `EditView` (save with the hash the file was read with, keeping the file's CRLF if it had it; conflict banner offering reload or overwrite; dirty mark; read-only when truncated; unsaved text is kept as a draft in the store, so any way of leaving, tab, back button or route change, is recoverable and only closing the tab warns; the editor module is loaded on demand so other screens do not carry CodeMirror's commands), `NewSessionView`, `SettingsView` (`DevicesSection`, `FluxSettingsForm`, `AgentConfigEditor`), `StatusBar` (connection; the open session's context-window usage as `ctx 239k`, plus `· 24%` when the box knew the model's window, the percentage amber from 70% and red from 90%; rate-limit windows on one line as `5h 13% ↻ 2h10m · 7d 24% ↻ 11h`, with `five_hour` and `seven_day` shortened and any other window named with spaces only when it is the most used, each window's renewal shown as a short relative time recomputed once a minute, only the most used window keeping its renewal on a phone-width bar, and the group tapped to add a line of absolute local times; push offer; last error with a labelled × to dismiss it), `ArchivedSessions` (on the list screen, a collapsed Archived section: Reopen, disabled with a hint when the worktree is gone, and Delete through the same confirm), `EventItem` (messages as bubbles, a user bubble listing its attachments underneath through `MessageAttachments`: a thumbnail for an image the store has fetched, which opens full-size in an overlay on tap, else the name and size; tools with their detail behind a tap, lifecycle rows as one-line notes, `task.started` as a tappable note ("Task: Explore · List directory files") that opens that agent's chat and `task.ended` as a collapsed disclosure holding the subagent's report, which is what the parent actually read, `session.cleared` as a rule across the timeline reading Context cleared, `pr.published` as a link, `hook.failed` as a warning with stderr behind a disclosure, unknown types as their name with the payload behind a tap). The "Thinking…" spinner is a `.loader` class in `styles/base.css` (ADR 0010 § Dependencies: no spinner package), foreground and accent tokens, static under `prefers-reduced-motion`.
- `git/`: `conventionalCommit` (`matches` a `type(scope)?!?: subject` line of at most 100 characters; `latest` picks the commit that seeds the PR title from a `git.log` page, which lists only the session's own commits beyond its base, so a session that has not committed yet falls back to the session title).
- `markdown/`: the timeline's own Markdown renderer for messages on both sides and the streaming bubble (`tokeniseMarkdown` for blocks, `inlineMarkdown` for runs, `renderMarkdown` to VNodes with `h()`). Deliberately partial: paragraphs with line breaks kept, `#`–`###` headings as bold lines, one level of nested list, fenced code (language as a class, no highlighting), `>` quotes, pipe tables (with column alignment, scrolling sideways when wide), inline code, bold, italic and `http(s)` links opened in a new tab with `noopener noreferrer`; everything else is literal text. No package (engineering.md § Dependencies) and no `v-html`: the agent's text only ever becomes text nodes, so it cannot inject markup. An unclosed fence renders as an open code block, which is what streaming shows mid-reply. The operator's own messages go through the same renderer, since they often paste agent output back.
- `editor/`: the CodeMirror unified diff (ADR 0005), the plain text editor behind `EditView` (no language packs yet), selection-to-line-range mapping, and `editor-theme.ts`, the one CodeMirror theme both editors use: dark-only like the app, built from the custom properties in `styles/base.css` (they inherit into the shadow roots), covering the gutters, active line and its line number, the native caret and selection (neither editor draws its own), and the merge view's changed and deleted chunks (the current-side rules scoped to `.cm-merge-b`, as the merge package's own are, so they win on specificity). CodeMirror itself is built into one `codemirror-*.js` chunk (`vite.config.ts`, `advancedChunks`) shared by the diff view and the lazily loaded file editor, with only `@codemirror/commands` left in the editor's chunk. Both editors wrap long lines (`EditorView.lineWrapping`; in the diff the deleted-chunk widgets sit under `.cm-content` and wrap with it), so nothing scrolls sideways on a phone; line ranges and gutter taps work on document positions, unaffected by wrapping. Both editors are mounted inside a shadow root: CodeMirror injects its styles as a `<style>` element, which the relay's CSP (`default-src 'self'`, no `unsafe-inline`) blocks in the document, while in a shadow root it uses a constructed stylesheet, which CSP does not govern.
- `sw.ts`: the service worker, built by Vite as a second entry to `/sw.js` so it is type-checked and linted like everything else. Push → notification; tap → open `/s/<id>`. No fetch caching at P1.
- `styles/base.css`: the only global stylesheet, linked from `index.html`. Colours are custom properties.

## Sync model

- Box is the only source of truth. No buffering on the relay. If the box is offline the PWA shows cached state and a disconnected banner.
- On connect (or reconnect) the PWA sends `events.sync { session, since: lastSeq }` for each open session. The daemon replays `seq > since`. Because `seq` is gapless the client detects any gap and re-syncs.
- Live events are pushed as they are appended. The client applies an event only if `seq === lastSeq + 1`, otherwise it triggers a sync. This makes duplicates and reordering harmless.
- Ephemeral deltas carry the `seq` of the `msg.assistant` they will become, so the client can discard them when the final message arrives.

## Notifications

Triggered by the daemon on `ask` and on `session.state idle` (after `running`) and on `notify done|blocked`. The daemon holds the subscriptions (`push.subscribe`) and sends Web Push itself, encrypted per RFC 8291 and signed with its VAPID key (`adr/0013`). Content stays minimal: session id and event type. The service worker opens the right session on tap.

## Failure modes

| Failure            | Behaviour                                                                              |
| ------------------ | -------------------------------------------------------------------------------------- |
| Relay down         | Daemon and PWA both reconnect with backoff. Nothing lost; box keeps logging.           |
| Box down           | PWA shows cached state, disconnected. No pretence of being live.                       |
| Daemon restarts    | Sessions resumed via agent `--resume`. Event log intact. PWA re-syncs.                 |
| Agent process dies | `session.state ended` with reason. Operator can restart from PWA.                      |
| Phone backgrounded | Socket dies. Push still arrives. Reopen → sync.                                        |
| Paired device lost | Revoke it from another device's Settings screen, or `flux devices rm <id>` on the box. |

## Deployment

`pnpm run build` runs each package's `build` in dependency order (daemon, relay, pwa; the protocol package is consumed from source and bundled, so it has no build). tsdown config lives in `apps/daemon/tsdown.config.ts` and `apps/relay/tsdown.config.ts`; `@flux/protocol` is bundled into both so neither built app depends on the workspace, node built-ins stay external. `deploy/` holds the systemd units, a Caddyfile and `.env.example` (every `FLUX_*` variable); `README.md` is the quickstart.

- Box: `apps/daemon/dist/index.mjs` (`bin: flux`), `dist/flux-mcp.mjs` (`bin: flux-mcp`, the MCP server the daemon points Claude at) and `dist/flux-pi-extension.mjs` (the pi extension with the same tools); both resolved as siblings of `index.mjs`, or of their `.ts` source when run from source. No runtime dependencies. `deploy/flux-daemon.service` runs it as the `flux` user with `Restart=always` and the hardening a box that runs agents can bear (no privilege escalation, no kernel or `/usr` writes; the home stays writable because agents work there, and there is no system-call filter because Claude Code's sandbox needs mount and user namespaces). The daemon prints the pairing QR at start only on a terminal; under systemd the operator runs `flux pair`. Requires `git` and whichever of `claude` and `pi` the operator wants to run on PATH (or `FLUX_CLAUDE`/`FLUX_PI`; the daemon checks once at start, says so in `hello` and on its console, and refuses `sessions.create` for an agent it did not find with `agent_unavailable`), and `gh` (logged in as the `flux` user) for opening pull requests from the PWA.
- Relay: `apps/relay/dist/index.mjs`, a single Node process bound to `127.0.0.1:8787` behind Caddy (`deploy/Caddyfile`, automatic TLS, HSTS, WebSocket passthrough) with `FLUX_TRUST_PROXY=1`. `hono`, `@hono/node-server` and `ws` are external, so the checkout keeps its `node_modules`. `deploy/flux-relay.service` runs it fully sandboxed (read-only system, no home, no devices). Serves PWA from `apps/pwa/dist`.
- Releases: a push of tag `vX.Y.Z` runs `.github/workflows/release.yml` (only on `v*` tags, so it is separate from the `ci.yml` gate). It builds every package, drafts a GitHub Release with the three daemon files and `manifest.json` (written by `scripts/sign-release.mjs --manifest-only`, no key on the runner), and builds `Dockerfile.relay` — a multi-stage image bundling the built relay, its three runtime deps and the built PWA — pushing it to `ghcr.io/musqet-rich/flux-relay:X.Y.Z` and `:latest`. The operator then runs `scripts/sign-and-publish.mjs vX.Y.Z` to sign the manifest offline and publish, and redeploys the image in Coolify. The full runbook is `releases.md`.
- PWA: built once by Vite, served by the relay. No separate hosting. For development, `pnpm --filter @flux/pwa dev` serves the source with HMR on :5173 and proxies `/ws` and `/healthz` to a relay at `FLUX_DEV_RELAY` (default `http://127.0.0.1:8787`), so the page origin still resolves to the box; the service worker is not registered under the dev server (`README.md` § Development).
- `apps/daemon/test/built-daemon.test.ts` builds the daemon into a temp dir under the real config and runs both files, so `pnpm run check` fails if the production build breaks.
- `e2e/` runs this whole shape on one machine: the built relay serving the built PWA on an ephemeral port, the built daemon in a temp data dir with `FLUX_CLAUDE` pointing at the fixture-replaying fake, pairing through the URL `flux pair` prints, and Chromium under Playwright as the device (`docs/engineering.md` § Testing).

## Out of scope for this document

Wire format, crypto and schemas: `protocol.md`. Tooling and rules: `engineering.md`. Why each decision: `adr/`.
